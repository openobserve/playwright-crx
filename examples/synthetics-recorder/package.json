{
  "name": "synthetics-recorder",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "vite build && vite build --config vite.content.config.ts",
    "build:content": "vite build --config vite.content.config.ts",
    "dev": "vite build --watch & vite build --config vite.content.config.ts --watch & wait"
  },
  "dependencies": {
    "playwright-crx": "file:../.."
  },
  "devDependencies": {
    "@types/chrome": "^0.0.281",
    "typescript": "^5.7.3",
    "vite": "^6.1.0"
  }
}
